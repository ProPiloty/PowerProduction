import React from 'react';
import {
  SafeAreaView,
  StyleSheet,
  ScrollView,
  View,
  Text,
  StatusBar,
} from 'react-native';

const Auth = () => {
  return (
    <View>
      <Text>Auth</Text>
    </View>
  );
};

const styles = StyleSheet.create({

});

export default Auth;